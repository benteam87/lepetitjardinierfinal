{% extends 'base.html.twig' %}

{% block title %}Haie{% endblock %}

{% block content %}
    <h1>Haie</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Code</th>
            <td>{{ haie.code }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ haie.nom }}</td>
        </tr>
        <tr>
            <th>Prix</th>
            <td>{{ haie.prix }}</td>
        </tr>
        <tr>
            <th>Categorie</th>
            <td>{{ haie.categorie }}</td>
        </tr>
        </tbody>
    </table>

    <div class="btn">
        <a href="{{ path('app_haie_index') }}">Retour à la liste</a>
        |
        <a href="{{ path('app_haie_edit', {'code': haie.code}) }}">Editer</a>

    </div>
    {{ include('haie/_delete_form.html.twig') }}
{% endblock %}
